namespace Nextflow.Domain.Enums;

public enum PurchaseStatus : byte
{
    Budget = 1,      // Orçamento
    Pending = 2,     // Pendente
    Received = 3,    // Recebido
    Canceled = 4     // Cancelado
}
